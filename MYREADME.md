# JSL04 Project Brief: Dynamic Task Display & Modal View

## Description 📜:

We were required to create a modal for our tasks, building and fixing upon a given solution. This project involves linking two JavaScript files together and further linking them to the rest of the project (linking through your html file, for it to successfully render your commands). We were also to add to its responsiveness (tablet and mobile view) and reinforce its desktop responsiveness - through adding and editing code where necessary.
This project will need to showcase the use of HTML, CSS and JavaScript with proper usability and functionality. While protecting the final product from unnecessary syntax errors (by proof reading our code).

## Task 🖊️:

We were tasked to successfully input the given coding solution of JSL03 (the HTML, CSS and JavaScript files) into our JSL04 project (whether by downloading/ copying, and pasting the files in). Then, we were to proceed by completing all the code for the files, for interactivity and useability. Lastly, we needed to add a professional README - with a thorough breakdown of our project requirements and process.

## Techonolgy 💻:

- HTML
- CSS
- JavaScript

## Features 🎨:

- A modal with input interactivity capabilities, through its form format.
- An "X" exit button, with the option the click outside of the modal space to shutdown the modal pop-up.
- A status scroll bar for status activity (that the user can change).
- A dark background, created through the :backdrop feature in CSS.

## Setup ⚙️:

CodeSpace gave us a file, that I cloned and forked from Git. I copied the link into VS Code and linked it into my JSL Projects file.